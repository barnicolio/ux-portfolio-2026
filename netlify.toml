[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback: serve index.html for client-side routes (/about, /work/:slug, …)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
